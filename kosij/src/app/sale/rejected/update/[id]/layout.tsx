export const metadata = {
  title: "KOISJ - Update Customize Trip",
};

export default function NotificationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
