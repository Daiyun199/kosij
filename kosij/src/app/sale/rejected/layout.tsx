export const metadata = {
  title: "KOISJ - Rejected Trip Request",
};

export default function NotificationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
