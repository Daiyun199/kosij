export const metadata = {
  title: "KOISJ - Consulting Staff List",
};

export default function NotificationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
