export const metadata = {
  title: "KOISJ - Custom Trip Booking",
};

export default function NotificationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
