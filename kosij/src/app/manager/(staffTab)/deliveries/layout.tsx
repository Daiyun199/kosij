export const metadata = {
  title: "KOISJ - Delivery Staff List",
};

export default function NotificationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
